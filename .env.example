# Copy this file to ".env" (same folder) and fill in your real values.
# ".env" is where config.py reads NCBI_EMAIL / NCBI_API_KEY from - it is
# never shown in the UI and never sent anywhere except to NCBI's own API.

NCBI_EMAIL=you@example.com
NCBI_API_KEY=your_ncbi_api_key_here
