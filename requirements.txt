streamlit>=1.32
requests>=2.31
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Optional but recommended: enables true dense-vector semantic re-ranking
# (SentenceTransformer). If not installed, the app automatically falls back
# to a TF-IDF cosine similarity re-ranker so it still runs end-to-end.
# sentence-transformers>=2.6
